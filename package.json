{
  "name": "ase",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . ASE --platform=win32 --arch=x64 --out=dist --overwrite",
    "package-mac": "electron-packager . ASE --platform=darwin --arch=x64 --out=dist --overwrite"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-packager": "^17.1.2"
  }
}
